import cv2
import numpy as np
import matplotlib.pyplot as plt
img = cv2.imread('名字.jpg')
H = np.float32([[1,0,500],[0,1,500]])
rows,cols = img.shape[:1]
res = cv2.warpAffine(img,H,(rows,cols)) #需要图像、变换矩阵、变换后的大小
plt.subplot(121)
plt.imshow(img)
plt.subplot(122)
plt.imshow(res)
